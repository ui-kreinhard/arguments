function build(data, provider, request, model, kaese) {

	interlink.prefill();
}

function interlrink(data, provider) {

}

Function.prototype.prefill = function(arguments) {
// extract arguments of calle
//
// extract arguments of yourself
//
// take only the matching names
// do it and fill it with it
//
// return it 
}
